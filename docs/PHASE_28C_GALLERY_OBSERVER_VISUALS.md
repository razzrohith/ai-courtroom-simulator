# Phase 28C – Gallery / Observer Visuals

## User Request
- Add a visual **gallery / observer** area to the courtroom stage.
- Must render professional Indian courtroom observer silhouettes, a **Review Panel** label, and subtle note‑taking animation.
- Should not be called a real Indian jury.
- Must integrate without blocking existing stage elements (judge bench, attorneys, live discussion, `StageEvidencePresenter`, etc.).
- Preserve all existing features from previous phases (multilingual selector, evidence presenter, autoplay, voice/TTS, provider configuration, etc.).

## Implementation Summary
- **Component**: `src/components/visuals/CourtroomGallery.tsx` – renders rows of observer silhouettes and a review‑panel overlay.
- **Styling**: `src/components/visuals/CourtroomGallery.module.css` – positions the gallery as a faint background layer, defines silhouette shapes, a subtle note‑taking animation, and the review‑panel style.
- **Integration**: Imported `CourtroomGallery` into `CourtroomStage.tsx` and inserted it at the top of the main stage container so it sits behind other UI elements.
- **Design**: Warm wood / muted gold / dark navy palette, non‑childish silhouettes, gentle animation that mimics observers taking notes.
- **Safety**: No external assets, no copyrighted images, no real jury references.

## Files Changed
- `src/components/visuals/CourtroomGallery.tsx` (new)
- `src/components/visuals/CourtroomGallery.module.css` (new)
- `src/components/visuals/CourtroomStage.tsx` (import and component insertion)
- `docs/PHASE_28C_GALLERY_OBSERVER_VISUALS.md` (this documentation)

## Verification
- **TypeScript**: `npm run typecheck` passes with no errors.
- **Compilation**: `npm run build` succeeds; the bundle includes the new gallery component.
- **Manual QA**: Verified in the browser that:
  - The gallery appears faintly behind the stage.
  - Silhouettes animate subtly (note‑taking pulse).
  - The **Review Panel** label is visible in the lower‑right corner.
  - No UI elements (judges, attorneys, evidence cards, live transcript) are obstructed.
  - Multilingual selector, `StageEvidencePresenter`, autoplay, voice/TTS, and Provider Configuration continue to function.
  - Mobile view (390 px width) still shows all elements without overlap.

## Known Limitations
- The observer silhouettes are simple geometric shapes; they are not photo‑realistic, but meet the design constraints.
- Advanced interaction (e.g., reacting to evidence phases) is limited to the basic pulse animation.

## Recommended Next Phase
- **Phase 28D** – Implement dynamic observer reactions (e.g., subtle head turns, facial expressions) synchronized with courtroom events.

---
*Generated by Antigravity agent.*
